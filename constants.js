// Shared constants for the extension

// Use var to avoid "Identifier has already been declared" errors if the script is injected multiple times
var LOGGING_LEVELS = {
  MINIMAL: 0,
  STANDARD: 1,
  DETAILED: 2,
  VERBOSE: 3
};
